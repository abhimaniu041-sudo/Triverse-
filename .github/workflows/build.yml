name: Build TriVerse APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Extract ZIP
      run: unzip TriVerse_Final.zip

    - name: Setup Java
      uses: actions/setup-java@v3
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    - name: Fix Code and Environment
      run: |
        mv TriVerse_Final triverse_final || true
        cd triverse_final
        rm -rf android
        flutter create . --platforms android
        # Code Patch: MainLayout missing import fix
        find . -name "home_dashboard.dart" -exec sed -i '1i import "package:triverse/widgets/main_layout.dart";' {} + || true
        flutter pub get

    - name: Final Build
      run: |
        cd triverse_final
        # Icons error skip karne ke liye flag add kiya hai
        flutter build apk --release --no-tree-shake-icons --no-sound-null-safety

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: TriVerse-App-Release
        path: 'triverse_final/build/app/outputs/flutter-apk/app-release.apk'
