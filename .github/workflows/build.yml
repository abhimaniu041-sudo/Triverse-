name: Build TriVerse APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Extract ZIP
      run: unzip TriVerse_Final.zip

    - name: Rename and Nuclear Fix
      run: |
        # Folder rename karna (Lowercase rule ke liye)
        mv TriVerse_Final triverse_final || true
        cd triverse_final
        # PURANA ANDROID FOLDER DELETE KARNA (Yahi problem ki jadd hai)
        rm -rf android
        # NAYA ANDROID FOLDER BANANA (Latest settings ke sath)
        flutter create . --platforms android
        flutter pub get

    - name: Setup Java
      uses: actions/setup-java@v3
      with:
        distribution: 'zulu'
        java-version: '17'

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    - name: Final Build
      run: |
        cd triverse_final
        flutter build apk --release --no-tree-shake-icons

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: TriVerse-App-Release
        path: '**/*.apk'
