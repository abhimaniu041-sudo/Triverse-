- name: Build APK (Ignoring Errors)
        run: |
          flutter pub get
          # Ye command errors ke bawajood build karne ki koshish karegi
          flutter build apk --release --no-tree-shake-icons --no-pub --dart-define=ignore-errors=true || flutter build apk --release --no-tree-shake-icons
